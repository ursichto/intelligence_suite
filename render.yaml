services:
  - type: web
    name: intelligence-suite
    env: python
    region: frankfurt  # or 'oregon', 'singapore', etc.
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn tis_dual_mode_summariser:app --host 0.0.0.0 --port 10000"
    plan: free
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # you’ll manually add this in the Render dashboard

